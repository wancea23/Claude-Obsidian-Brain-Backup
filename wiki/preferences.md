# Coding Preferences & Style

> Observed from codebase patterns. Updated as new preferences are confirmed.

---

## Languages Used
- **Primary**: Python, JavaScript/HTML/CSS, Java, C
- **Secondary**: Luau (Roblox), Node.js

## Project Structure Preferences
- Likes packaging Python apps as executables (PyInstaller `.spec` files)
- Prefers flat structure for scripts; subfolder structure for lab work
- University lab code organized by subject (`PC`, `SDA`, `NA`, `POO`)

## Coding Patterns
- Python: Uses `__pycache__` → runs scripts directly, not just editing
- JS: Writes utility scripts as standalone files (`fetch.py`, `extract.py`, etc.)
- Exploratory scripting: Many one-off files (`solve.js`, `solve3.js`, `fetch_css.js`)
- Data work: CSV exports for laptop price comparison data

## Tools & Workflow
- Uses Claude Code as AI assistant
- Uses Claude Code's swarm/ruflo integration for complex tasks
- Obsidian for knowledge management (this vault)
- PyInstaller for packaging desktop apps
- Node.js for web scraping and site generation

## AI Collaboration Preferences (from CLAUDE.md rules)
- Do what was asked; nothing more, nothing less
- Never create unnecessary files
- Always prefer editing existing files
- Never save working files to root folder
- Always read files before editing
- All operations should be concurrent/parallel in one message

---

## Notes
- Add confirmed preferences here as they are observed
- See [[mistakes]] for patterns to avoid
